{
  "name": "recive-movie-api",
  "version": "1.0.0",
  "main": "index.js",
  "author": "Americo",
  "license": "MIT",
  "scripts": {
    "start": "ts-node -r dotenv/config -r tsconfig-paths/register ./dist/accounts-service/src/server",
    "compile": "tsc && ts-node -r dotenv/config -r tsconfig-paths/register ./dist/accounts-service/src/server",
    "dev:luiz": "nodemon -e ts,json --ignore dist --exec \"yarn run compile\"",
    "dev": "ts-node-dev --inspect -r dotenv/config --transpile-only --ignore-watch node_modules src/app.ts",
    "test": "jest"
  },
  "dependencies": {
    "axios": "^0.21.1",
    "body-parser": "^1.19.0",
    "dotenv-safe": "^8.2.0",
    "express": "^4.17.1",
    "helmet": "^4.4.1"
  },
  "devDependencies": {
    "@types/express": "^4.17.11",
    "@types/node": "^14.14.31",
    "nodemon": "^2.0.7",
    "ts-node-dev": "1.1.6",
    "@types/axios": "^0.14.0",
    "typescript": "3.4.3"
  }
}
